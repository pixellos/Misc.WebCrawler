@using WebCrawler.Model.Benchmarking
@model IEnumerable<EntryViewModel>

@{ 
    Layout = "";
}

<div class="col s10">
    <div class="row">
        <div class="col s4">
            @nameof(EntryViewModel.Uri)
        </div>
        <div class="col s4 offset-s4">
            @nameof(EntryViewModel.Miliseconds)
        </div>
    </div>
    @foreach (var item in @Model)
    {
        <div class="row">
            <div class="col s4">@item.Uri </div>
            <div class="col s4 offset-s4">@item.Miliseconds </div>
        </div>
    }
</div>
